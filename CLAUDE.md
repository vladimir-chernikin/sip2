# 🤖 Claude Code - Инструкции для работы с проектом

## ⚠️ КРИТИЧЕСКИ ВАЖНОЕ ПРАВИЛО

**ВСЕ изменения должны быть закоммичены сразу после внесения!**

Каждое изменение в:
- 📝 Программном коде (Python, JS, и т.д.)
- ⚙️ Конфигурационных файлах (docker-compose.yml, .env)
- 📋 Файлах настроек Asterisk (pjsip.conf, extensions.conf, etc.)
- 🐳 Dockerfile и Docker скриптах
- 📚 Документации (README.md, инструкции)

**ДОЛЖНО быть немедленно закоммичено в Git!**

---

## 🔄 Обязательный процесс работы

### При внесении ЛЮБЫХ изменений:

```bash
# 1. Внесите изменения (редактирование файлов)
# ...

# 2. Проверьте что изменилось
git status

# 3. Добавьте изменённые файлы
git add .

# 4. Сделайте коммит с ОПИСАТЕЛЬНЫМ сообщением
git commit -m "описание что и зачем изменил"

# 5. Запушьте в GitHub
git push
```

---

## 📝 Правила для коммитов

### Хороший коммит:
```bash
git commit -m "fix: исправлен VAD silence_duration_ms с 200 на 800мс для более точного детектирования конца речи"
```

### Плохой коммит:
```bash
git commit -m "update"
```

### Формат сообщения:
```
<тип>: <краткое описание>

<детальное описание если нужно>

Типы:
- fix    - исправление ошибки
- feat   - новая функциональность
- config - изменение конфигурации
- docs   - изменение документации
- refactor - рефакторинг кода
- test   - добавление тестов
```

---

## 🚫 ЧТО НЕЛЬЗЯ ДОПУСКАТЬ

1. ❌ Накапливать незакоммиченные изменения
2. ❌ Делать изменения без коммита "потом закоммичу"
3. ❌ Пушить без предварительного коммита
4. ❌ Оставлять изменения в рабочей директории

---

## 📋 Чек-лист перед завершением работы

```bash
# 1. Проверьте статус
git status
# Должно быть: "nothing to commit, working tree clean"

# 2. Если есть изменения - закоммитьте!
git add .
git commit -m "описание изменений"
git push

# 3. Убедитесь что всё чисто
git status
```

---

## 🔥 Автоматизация (опционально)

### Добавить в ~/.bashrc:

```bash
alias gg='git add . && git commit -m "update: $(date +%H:%M)" && git push'
```

Использование:
```bash
# После изменений просто введите:
gg
```

---

## 📊 Мониторинг состояния

### Проверить что НЕ закоммичено:
```bash
git status
git diff
```

### История коммитов:
```bash
git log --oneline --graph --all
```

---

## ⚡ Быстрые команды

```bash
# Статус
git status

# Добавить всё
git add .

# Коммит
git commit -m "message"

# Пуш
git push

# Всё сразу (если есть изменения)
git add . && git commit -m "message" && git push
```

---

## 🎯 ПРИМЕР РАБОТЫ

```bash
# 1. Редактируем файл
nano /asterisk/media_sockets/instructions.md

# 2. Сразу коммитим
cd /asterisk
git add .
git commit -m "docs: добавлено правило об обязательном приветствии"
git push

# 3. Продолжаем работу
```

---

**ПОМНИ: Каждый коммит - это сохранение прогресса!**
