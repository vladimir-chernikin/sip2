# 🤖 Claude Code - Инструкции для работы с проектом

## ⚠️ КРИТИЧЕСКИ ВАЖНОЕ ПРАВИЛО

**ВСЕ изменения должны быть закоммичены сразу после внесения!**

Каждое изменение в:
- 📝 Программном коде (Python, JS, и т.д.)
- ⚙️ Конфигурационных файлах (docker-compose.yml, .env)
- 📋 Файлах настроек Asterisk (pjsip.conf, extensions.conf, etc.)
- 🐳 Dockerfile и Docker скриптах
- 📚 Документации (README.md, инструкции)

**ДОЛЖНО быть немедленно закоммичено в Git!**

---

## 🔄 Обязательный процесс работы

### При внесении ЛЮБЫХ изменений:

```bash
# 1. Внесите изменения (редактирование файлов)
# ...

# 2. Проверьте что изменилось
git status

# 3. Добавьте изменённые файлы
git add .

# 4. Сделайте коммит с ОПИСАТЕЛЬНЫМ сообщением
git commit -m "описание что и зачем изменил"

# 5. Запушьте в GitHub
git push
```

---

## 📝 Правила для коммитов

### Хороший коммит:
```bash
git commit -m "fix: исправлен VAD silence_duration_ms с 200 на 800мс для более точного детектирования конца речи"
```

### Плохой коммит:
```bash
git commit -m "update"
```

### Формат сообщения:
```
<тип>: <краткое описание>

<детальное описание если нужно>

Типы:
- fix    - исправление ошибки
- feat   - новая функциональность
- config - изменение конфигурации
- docs   - изменение документации
- refactor - рефакторинг кода
- test   - добавление тестов
```

---

## 🚫 ЧТО НЕЛЬЗЯ ДОПУСКАТЬ

1. ❌ Накапливать незакоммиченные изменения
2. ❌ Делать изменения без коммита "потом закоммичу"
3. ❌ Пушить без предварительного коммита
4. ❌ Оставлять изменения в рабочей директории

---

## 📋 Чек-лист перед завершением работы

```bash
# 1. Проверьте статус
git status
# Должно быть: "nothing to commit, working tree clean"

# 2. Если есть изменения - закоммитьте!
git add .
git commit -m "описание изменений"
git push

# 3. Убедитесь что всё чисто
git status
```

---

## 🔥 Автоматизация (опционально)

### Добавить в ~/.bashrc:

```bash
alias gg='git add . && git commit -m "update: $(date +%H:%M)" && git push'
```

Использование:
```bash
# После изменений просто введите:
gg
```

---

## 📊 Мониторинг состояния

### Проверить что НЕ закоммичено:
```bash
git status
git diff
```

### История коммитов:
```bash
git log --oneline --graph --all
```

---

## ⚡ Быстрые команды

```bash
# Статус
git status

# Добавить всё
git add .

# Коммит
git commit -m "message"

# Пуш
git push

# Всё сразу (если есть изменения)
git add . && git commit -m "message" && git push
```

---

## 🎯 ПРИМЕР РАБОТЫ

```bash
# 1. Редактируем файл
nano /asterisk/media_sockets/instructions.md

# 2. Сразу коммитим
cd /asterisk
git add .
git commit -m "docs: добавлено правило об обязательном приветствии"
git push

# 3. Продолжаем работу
```

---

**ПОМНИ: Каждый коммит - это сохранение прогресса!**

## 🔑 КРИТИЧЕСКИ ВАЖНАЯ ИНФОРМАЦИЯ О СИСТЕМЕ

### 📞 SIP Транки и Клиенты

**1001 и Билайн - это РАЗНЫЕ SIP транки/клиенты!**

1. **1001** - внутренний SIP клиент (MicroSIP/Zoiper) для тестирования
   - Endpoint: `PJSIP/1001`
   - Используется для тестирования с локального компьютера
   - Может участвовать в разговоре или не участвовать

2. **Билайн** - внешний SIP транк (ip.beeline.ru:5060)
   - Endpoint: `PJSIP/beeline` 
   - Используется для реальных звонков на мобильные/городские
   - Номер: 713433506036
   -_credentials: SIP030FQU0451O / ZAQ12wsx-

**Они могут быть в одном bridge или нет - это разные каналы!**

Пример bridge:
```
Bridge: mixing
  Channel 1: PJSIP/beeline-00000001  (входящий от Билайн)
  Channel 2: UnicastRTP/...         (аудио к AI)
  Channel 3: PJSIP/1001-00000002   (опционально, если подключён MicroSIP)
```

### 🚨 ПРОБЛЕМА ЗАВИСШИХ КАНАЛОВ (КРИТИЧНО!)

**Симптомы:**
- Множество каналов в состоянии `Up Stasis` с временем > 5 минут
- `rtpend` достигает лимита → новые звонки не создаются
- Ошибка: `we couldn't allocate a port for RTP instance`

**Откуда берутся:**
1. Входящий звонок от Билайна → Stasis → ExternalMedia
2. OpenAI отвечает "Здравствуйте!"
3. **RTP не доходит до телефона** (пользователь не слышит)
4. Пользователь кладёт трубку через 5-10 секунд
5. **Backend НЕ обрабатывает Hangup!** Канал остаётся в `Up Stasis` навсегда

**Как обнаружить:**
```bash
docker exec asterisk asterisk -rx "core show channels" | grep "Up.*Stasis" | grep -E "[0-9]{2}:[0-9]{2}"
```

Если видите время > 5 минут - это зависшие каналы!

**Как исправить:**
```bash
# Убить все зависшие каналы
docker exec asterisk asterisk -rx "channel request hangup all"
```

**Как предотвратить:**
Backend ДОЛЖЕН обрабатывать события:
- `StasisEnd` - когда канал покидает Stasis
- `ChannelDestroyed` - когда канал уничтожен
- `ChannelHangupRequest` - когда запрашивают Hangup

### 🔧 RTP порты

**Стандартные настройки:**
- `rtpstart=10000`
- `rtpend=10100` (100 портов = 50 одновременных звонков)
- **Этого достаточно для тестирования!**

**Увеличивать НЕ НУЖНО** - нужен правильный cleanup каналов!

---

## 📁 ПРАВИЛЬНАЯ РАБОТА С ФАЙЛАМИ В CLAUDE CODE

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Не используйте Bash с heredoc!

❌ **НЕПРАВИЛЬНО (вызывает ошибку):**
```javascript
Bash(cat > /path/to/file.txt << 'EOF'
текст файла
EOF)
```

**Почему это плохо?**
- Claude автоматически добавляет выполненную команду в `settings.local.json`
- Heredoc-конструкция (`<< 'EOF'...EOF`) вызывает ошибку парсера
- Ошибка: `The :* pattern must be at the end...`

---

### ✅ ПРАВИЛЬНЫЕ СПОСОБЫ:

#### 1. Создание нового файла:
```javascript
// Сначала прочитай, если файл существует
Read tool: /path/to/file.txt

// Создай файл
Write tool
  file_path: "/path/to/file.txt"
  content: "содержимое файла"
```

#### 2. Редактирование существующего файла:
```javascript
// 1. Сначала прочитай файл
Read tool: /path/to/file.txt

// 2. Затем редактируй
Edit tool
  file_path: "/path/to/file.txt"
  old_string: "старый текст"
  new_string: "новый текст"
```

#### 3. Интерактивное редактирование (если нужно):
```bash
Bash(nano /path/to/file)
Bash(vi /path/to/file)
```

---

### 📋 Правила для settings.local.json

✅ **Правильно:**
```json
{
  "permissions": {
    "allow": [
      "Bash(cat:*)",
      "Bash(nano:*)",
      "Bash(vi:*)"
    ]
  }
}
```

❌ **Неправильно:**
```json
{
  "permissions": {
    "allow": [
      "Bash(/path/to/file << 'EOF'\n...)"
    ]
  }
}
```

---

### 💡 Запомни:

| Задача | Правильный инструмент |
|--------|----------------------|
| Создать файл | `Write tool` |
| Редактировать файл | `Edit tool` (сначала `Read`) |
| Прочитать файл | `Read tool` |
| Запустить команду | `Bash(команда:*)` |

**НИКАКИХ heredoc-конструкций в Bash!**

